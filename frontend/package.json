{
  "name": "tapscribe-frontend-typecheck",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "tsc --noEmit gate for tapscribe/web/js/. Never shipped; devDep only.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
