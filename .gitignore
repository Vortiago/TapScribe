# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.venv/
venv/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.ruff_cache/

# Recorder state — gitignored entirely; created at runtime.
recordings/
.auth-password
.tap-token
.tapscribe-cert.pem
.tapscribe-key.pem
# Install-picker state — operator's chosen model families. Pre-checked on
# next run so re-launching is one Enter keystroke.
.tapscribe-install.json

# Live-benchmark results (tools/bench_live.py) — generated per run.
bench-results/

# Frontend typecheck tooling (devDep only, never shipped)
frontend/node_modules/
frontend/package-lock.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
