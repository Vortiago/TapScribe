# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.venv/
venv/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.ruff_cache/

# Recorder state — gitignored entirely; created at runtime.
recordings/
.auth-password
.tap-token
.tapscribe-cert.pem
.tapscribe-key.pem
# Install-picker state — operator's chosen model families. Pre-checked on
# next run so re-launching is one Enter keystroke.
.tapscribe-install.json

# Frontend typecheck tooling (devDep only, never shipped)
frontend/node_modules/
frontend/package-lock.json

# SpatialChat Bridge typecheck tooling (devDep only, never shipped)
bridges/spacialchat-bridge/typecheck/node_modules/
bridges/spacialchat-bridge/typecheck/package-lock.json

# SpatialChat Bridge popup E2E (Playwright; devDep only, never shipped)
bridges/spacialchat-bridge/e2e/node_modules/
bridges/spacialchat-bridge/e2e/package-lock.json
bridges/spacialchat-bridge/e2e/test-results/
bridges/spacialchat-bridge/e2e/playwright-report/
bridges/spacialchat-bridge/e2e/blob-report/

# Perf soak reports (tests/e2e/test_next_perf_soak.py) — local measurements
perf-reports/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Operator-editable summarizer default (PUT /api/summarize/config) — runtime
# state like recordings/, never committed. (The empty prompt/hotwords text
# files ARE tracked as starter files; this one is created on first save.)
config/summarizer.json
.bench-da-no.json
